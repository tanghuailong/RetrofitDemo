<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <ImageView
        android:id="@+id/logo"
        android:layout_width="85dp"
        android:layout_height="45dp"
        android:layout_alignParentLeft="true"
        android:layout_centerInParent="true"
        android:src="@drawable/sunlogo" />
    <View
        android:id="@+id/adjust_view"
        android:layout_width="20dp"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/logo"
        android:layout_height="16dp"></View>

    <ImageView
        android:layout_width="100dp"
        android:layout_height="55dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/sunsign" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="@string/sun_sign_name"
        android:textColor="@color/desc_text_color" />

    <LinearLayout
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@id/adjust_view"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:orientation="horizontal">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/status_bar_txt_color"
            android:textSize="30sp"
            android:text="@string/device_num"/>
        <TextView
            android:id="@+id/device_code"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/status_bar_txt_color"
            android:textSize="30sp"
            tools:text="G210"/>
    </LinearLayout>
</RelativeLayout>